package backend;

public interface MascotaObserver {
    /**
     * metodo que actualiza el obvserder de una mascota
     * @param mascota a cual observador actualizar
     */
    public void actualizar(Mascota mascota);
}
